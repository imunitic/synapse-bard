# The platforms synapse-bard ships prebuilt binaries for -- the single
# definition, sourced by release.yml so there is exactly one copy of this
# list to keep current.
targets=(x86_64-linux aarch64-linux aarch64-macos)
